# Copiar con sudo para activar ventiladores al arrancar (Gigabyte B760):
#   sudo cp scripts/load-fan-modules.conf /etc/modules-load.d/turing-fans.conf
#   sudo cp scripts/modprobe-fans.conf /etc/modprobe.d/turing-fans.conf
#   sudo modprobe nct6683
#   sudo modprobe it87 ignore_resource_conflict=1
it87